# Hello World

This example shows the current Python-facing API shape. It records route
metadata only; there is no production Rust execution bridge yet.

```bash
PYTHONPATH=../../python python app.py
```
