# Examples

Examples demonstrate intended Silta developer experience and current bootstrap
behavior.

Current examples:

- [hello-world](hello-world/README.md): minimal `App` route metadata example.

Examples that use proposed APIs must be clearly labeled as proposed and subject
to RFC. Do not add examples that imply a production runtime, ORM, CRUD layer, or
deployment engine exists before it does.
